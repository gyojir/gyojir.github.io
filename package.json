{
  "name": "gyojir.github.io",
  "scripts": {
    "dev": "nuxt dev --port 3333 --host 0.0.0.0",
    "generate": "nuxt build --preset github_pages",
    "deploy": "push-dir --dir=dist --branch=master --cleanup --verbose"
  },
  "dependencies": {
    "nuxt": "^3.12.2"
  },
  "devDependencies": {
    "@nuxtjs/google-fonts": "^3.2.0",
    "normalize.css": "^8.0.1",
    "nuxt-gtag": "^2.0.6",
    "push-dir": "^0.4.1",
    "sass": "^1.77.6"
  },
  "engines": {
    "node": "20.x"
  }
}
